Параметри для з'єднання з базою даних(host, port, dbname, user, password), посилання на 2 файли які потрібно завантажити в базу даних, та змінна N знаходяться в файлі config.py
SQL-скрипти знаходяться в файлі queries.py

Після запуску програми користувачу надається вибір: створити таблицю(якщо не існує) та записати туди файли, або зробити запит до бази даних згідно з завданням в лабораторній роботі
після запуску програма розбиває 2 вхідні файли на файли меншого розміру(до N строк кожен), програма створює папку dir в яку записує отримані файли, 
потім програма записує ці файли в бд, коли файл було записано та відбувся commit файл видаляється. Після того, як дані були записані виконується скрипт SELECT дані отримані під час 
роботи скрипта записуються в файл results.csv. Також цей скрипт можна виконати окремо.

При падінні бази даних(зупинці контейнеру в докері) програма зупиняється, та просить її перезапустити. Оскільки під час запису файлів в базу даних, кожний файл,
після того як був записаний, видаляється з папки dir, то після перезапуску програми програма просто записує решту файлів, що залишилися в папці dir.

